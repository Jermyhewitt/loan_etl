version: 2

sources:
  - name: etl_source
    database: etl_source
    schema: etl_source
    tables:
      - name: debtor
        description: "Table containing debtor information"
        columns:
          - name: user_id
            description: "Foreign key to the user table"
          - name: address_id
            description: "Foreign key to the address table"
          - name: loan_id
            description: "Foreign key to the loan table"